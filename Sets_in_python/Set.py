# set is mutable we can add or remove elements but the existing elements are immutable we cannot change them.
#Intro of sets in python. {} used to represent sets and values in sets cannot be repeated and unorderd.
set = {1,2,3,3,4,"vaibhav" ,"pandey"}
print(set)
print(type(set))
print(len(set)) # in this we can see set ignores the repeated value of 3. which shows immmutable nature of stes.

#To create empty set .
demo = set()
print(type(demo))